PORT=
VITE_API_BASE_URL=
MONGODB_URI=
JWT_SECRET=
# Chatbot (Groq). Optional for DB-only fallback answers; full AI needs a key from https://console.groq.com
GROQ_API_KEY=
# Optional: max tasks returned per chat list/export (default 200, max 500)
AI_CHAT_TASKS_LIMIT=
